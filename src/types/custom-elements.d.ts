// Optional: declare custom web components used in JSX to avoid JSX.IntrinsicElements errors.
// Only add tags you actually use. Example template:

declare namespace JSX {
  interface IntrinsicElements {
    // 'geo-map': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement> & { latitude?: number };
  }
}

export {};
